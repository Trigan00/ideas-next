import Landing from "./Landing";

export const metadata = {
  title: "Startup ideas",
  description: "Find the idea that will ignite your next project",
};

export default function Home() {
  return <Landing />;
}
